#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: first tinteger to be multiplied with.
 * @b: second integer to be multiplied with
 * Return: multiplication of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
